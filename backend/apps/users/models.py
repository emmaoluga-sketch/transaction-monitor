from django.contrib.auth.models import AbstractUser

# We are using Django's default User model, so we don't need to define anything here.
# But we keep this file to tell Django this is an app with models.
# For now, we just import AbstractUser to show it's available.